node_modules
dist
dist-preview
dist-ssr
*.local
.DS_Store
.vite
*.tsbuildinfo

# Local secrets
.env
.env.local
.env.*.local

# The compiled shadow of a test — the .ts is the file, and the test runner
# bundles it itself.
src/**/*.test.js
